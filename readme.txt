1st time commands:

- xhost +local:docker

setup: 
- extract everything from the zip except for node_modules(!) in GrandeOmega 
- run the build file using ./build

run the program:
- run the 'run' file using ./run

update the program:
- delete everything in the GrandeOmega directory
- redo the setup